import { IncomingMessage, ServerResponse } from 'http';
import { FastifyInstance } from 'fastify';
import { F as FrameworkContract } from '../../framework.contract-YnJ-fl1w.js';

/**
 * The framework that forwards requests to fastify handler
 *
 * @breadcrumb Frameworks / FastifyFramework
 * @public
 */
declare class FastifyFramework implements FrameworkContract<FastifyInstance> {
    /**
     * {@inheritDoc}
     */
    sendRequest(app: FastifyInstance, request: IncomingMessage, response: ServerResponse): void;
}

export { FastifyFramework };
