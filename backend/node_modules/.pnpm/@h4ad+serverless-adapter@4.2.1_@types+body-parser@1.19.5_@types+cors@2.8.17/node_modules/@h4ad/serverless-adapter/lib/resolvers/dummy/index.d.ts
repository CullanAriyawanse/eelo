import { R as ResolverContract, c as Resolver } from '../../adapter.contract-EQE3rlgO.js';
import '../../headers-K2qbcurv.js';
import 'http';
import 'node:http';
import '../../logger-9me5n48P.js';

/**
 * The class that represents a dummy resolver that does nothing and can be used by the cloud that doesn't use resolvers.
 *
 * @breadcrumb Resolvers / DummyResolver
 * @public
 */
declare class DummyResolver implements ResolverContract<any, any, any, any, any> {
    /**
     * {@inheritDoc}
     */
    createResolver(): Resolver<any, void>;
}

export { DummyResolver };
