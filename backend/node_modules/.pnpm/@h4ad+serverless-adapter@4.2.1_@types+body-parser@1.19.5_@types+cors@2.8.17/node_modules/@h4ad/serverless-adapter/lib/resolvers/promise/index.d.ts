import { R as ResolverContract, b as ResolverProps, c as Resolver } from '../../adapter.contract-EQE3rlgO.js';
import '../../headers-K2qbcurv.js';
import 'http';
import 'node:http';
import '../../logger-9me5n48P.js';

/**
 * The class that implements the resolver using the promise object sent by this library
 *
 * @breadcrumb Resolvers / PromiseResolver
 * @public
 */
declare class PromiseResolver<TEvent, TContext, TCallback, TResponse, TReturn> implements ResolverContract<TEvent, TContext, TCallback, TResponse, Promise<any>> {
    /**
     * {@inheritDoc}
     */
    createResolver({ event, log, respondWithErrors, adapter, }: ResolverProps<TEvent, TContext, TCallback, TResponse>): Resolver<TResponse, Promise<TReturn>>;
}

export { PromiseResolver };
