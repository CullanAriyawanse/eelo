import { IncomingMessage, ServerResponse } from 'http';
import Application from 'koa';
import { F as FrameworkContract } from '../../framework.contract-YnJ-fl1w.js';

/**
 * The framework that forwards requests to koa handler
 *
 * @breadcrumb Frameworks / KoaFramework
 * @public
 */
declare class KoaFramework implements FrameworkContract<Application> {
    /**
     * {@inheritDoc}
     */
    sendRequest(app: Application, request: IncomingMessage, response: ServerResponse): void;
}

export { KoaFramework };
