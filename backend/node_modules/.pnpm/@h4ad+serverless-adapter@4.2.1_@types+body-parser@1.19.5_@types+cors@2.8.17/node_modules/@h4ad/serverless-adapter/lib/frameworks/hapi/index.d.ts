import { IncomingMessage, ServerResponse } from 'http';
import { Server } from '@hapi/hapi';
import { F as FrameworkContract } from '../../framework.contract-YnJ-fl1w.js';

/**
 * The framework that forwards requests to hapi handler
 *
 * @breadcrumb Frameworks / HapiFramework
 * @public
 */
declare class HapiFramework implements FrameworkContract<Server> {
    /**
     * {@inheritDoc}
     */
    sendRequest(app: Server, request: IncomingMessage, response: ServerResponse): void;
}

export { HapiFramework };
